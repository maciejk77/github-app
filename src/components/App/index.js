import React from 'react';
import SearchBar from '../SearchBar';
import Repositories from '../Repositories';

const App = () => {
  return (
    <div>
      <SearchBar />
    </div>
  );
};

export default App;
